# -*- coding: utf-8 -*-
"""Copie de Bienvenue dans Colab

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1XfJ5sYtaN5vYNLxoQHU-bLPVf8vu2zHj
"""

pip install pandas numpy matplotlib scikit-learn pmdarima statsmodels

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from pmdarima import auto_arima
from sklearn.metrics import mean_absolute_error, mean_squared_error, mean_absolute_percentage_error
import warnings
warnings.filterwarnings("ignore")

# ==========================================
# 1. Chargement et préparation des données
# ==========================================
# Remplacement par le nom de ton nouveau fichier
fichier_csv = 'boissons.xlsx'

# Si les dates posent problème (ex: format européen JJ/MM/AAAA), ajoute dayfirst=True
df = pd.read_excel(fichier_csv)
df['Date'] = pd.to_datetime(df['Date'], format='mixed', dayfirst=True)

# Définir le produit à analyser (Vérifie qu'il existe bien dans ce nouveau fichier)
produit_cible = 'P001'
df_produit = df[df['Produit_ID'] == produit_cible].copy()

# Préparation de la série temporelle
df_produit.sort_values('Date', inplace=True)
df_produit.set_index('Date', inplace=True)
df_produit = df_produit.asfreq('D') # Fréquence quotidienne

# Nettoyage des valeurs (à adapter si tu as d'autres variables explicatives)
df_produit['Ventes_Jour'] = df_produit['Ventes_Jour'].interpolate()

# Vérifie que ces colonnes (Promo, Prix) existent dans ton nouveau dataset.
# Sinon, supprime la partie "X" (variables exogènes) partout dans le code.
if 'Promo' in df_produit.columns and 'Prix' in df_produit.columns:
    df_produit['Promo'] = df_produit['Promo'].fillna(0)
    df_produit['Prix'] = df_produit['Prix'].fillna(method='ffill')
    X = df_produit[['Promo', 'Prix']]
else:
    X = None # Mode ARIMA simple si pas de variables explicatives

y = df_produit['Ventes_Jour']

# ==========================================
# 2. Division Temporelle en 3 parties
# ==========================================
jours_test = 30  # Le dernier mois pour le test
jours_val = 30   # L'avant-dernier mois pour la validation

# Entraînement
train_y = y.iloc[:-(jours_test + jours_val)]
train_X = X.iloc[:-(jours_test + jours_val)] if X is not None else None

# Validation
val_y = y.iloc[-(jours_test + jours_val):-jours_test]
val_X = X.iloc[-(jours_test + jours_val):-jours_test] if X is not None else None

# Test
test_y = y.iloc[-jours_test:]
test_X = X.iloc[-jours_test:] if X is not None else None

print(f"--- DÉCOUPAGE DE LA BASE DE DONNÉES ---")
print(f"Taille Entraînement : {len(train_y)} jours")
print(f"Taille Validation   : {len(val_y)} jours")
print(f"Taille Test         : {len(test_y)} jours\n")

# ==========================================
# 3. Entraînement initial sur le jeu de Train
# ==========================================
print("Recherche du meilleur modèle en cours (cela peut prendre un moment)...")
modele_sarima = auto_arima(
    y=train_y,
    X=train_X,
    seasonal=True,
    m=7,                 # Saisonnalité de 7 jours
    trace=False,
    error_action='ignore',
    suppress_warnings=True,
    stepwise=True
)

# ==========================================
# 4. Phase de Validation
# ==========================================
pred_val = modele_sarima.predict(n_periods=jours_val, X=val_X)

# ==========================================
# 5. Mise à jour et Phase de Test final
# ==========================================
# On actualise le modèle avec les données de validation
modele_sarima.update(val_y, X=val_X)

# Prédiction sur le mois de test
pred_test = modele_sarima.predict(n_periods=jours_test, X=test_X)

# ==========================================
# 6. Évaluation de la précision (MAPE)
# ==========================================
def afficher_precision(y_vrai, y_pred, nom_phase):
    mae = mean_absolute_error(y_vrai, y_pred)
    mape = mean_absolute_percentage_error(y_vrai, y_pred) * 100
    precision_globale = 100 - mape

    print(f"--- RÉSULTATS : {nom_phase} ---")
    print(f"Marge d'erreur (MAE) : ±{mae:.2f} articles")
    print(f"Erreur moyenne (MAPE): {mape:.2f} %")
    print(f"🎯 Précision du modèle : {precision_globale:.2f} %\n")

afficher_precision(val_y, pred_val, "PHASE DE VALIDATION")
afficher_precision(test_y, pred_test, "PHASE DE TEST FINAL")

# ==========================================
# 7. Visualisation
# ==========================================
plt.figure(figsize=(15, 6))

# Afficher les derniers 90 jours d'entraînement
plt.plot(train_y.index[-90:], train_y.iloc[-90:], label='Entraînement', color='#1f77b4')
plt.plot(val_y.index, val_y, label='Réel (Validation)', color='#ff7f0e')
plt.plot(val_y.index, pred_val, label='Prédictions (Val)', color='#ff7f0e', linestyle='--')
plt.plot(test_y.index, test_y, label='Réel (Test)', color='#2ca02c')
plt.plot(test_y.index, pred_test, label='Prédictions (Test)', color='#d62728', linestyle='--')

plt.axvline(x=val_y.index[0], color='black', linestyle=':', alpha=0.5)
plt.axvline(x=test_y.index[0], color='black', linestyle=':', alpha=0.5)

plt.title("Prévision de la demande pour les Boissons", fontsize=14, fontweight='bold')
plt.xlabel("Date", fontsize=12)
plt.ylabel("Unités Vendues", fontsize=12)
plt.legend()
plt.grid(True, linestyle=':', alpha=0.6)
plt.tight_layout()
plt.show()

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_absolute_error, mean_squared_error, mean_absolute_percentage_error
import warnings
warnings.filterwarnings("ignore")

# ==========================================
# 1. Chargement et préparation des données
# ==========================================
fichier_csv = 'boissons.xlsx'

# CORRECTION 1 : On lit le fichier sans l'argument parse_dates pour éviter le crash immédiat
df = pd.read_excel(fichier_csv)

# CORRECTION 2 : Nettoyage des noms de colonnes (enlève les espaces invisibles au début et à la fin)
df.columns = df.columns.str.strip()

# CORRECTION 3 : Conversion manuelle de la date avec un système de sécurité
try:
    df['Date'] = pd.to_datetime(df['Date'], format='mixed', dayfirst=True)
except KeyError:
    print(f"🛑 ERREUR : La colonne 'Date' reste introuvable.")
    print(f"Voici les colonnes que Pandas a détectées dans ton fichier : {df.columns.tolist()}")
    raise KeyError("Vérifie le nom exact de la colonne dans ton fichier Excel.")

produit_cible = 'P001'
df_produit = df[df['Produit_ID'] == produit_cible].copy()

# Trier par date et configurer l'index temporel
df_produit.sort_values('Date', inplace=True)
df_produit.set_index('Date', inplace=True)
df_produit = df_produit.asfreq('D')

# Nettoyage des valeurs manquantes
df_produit['Ventes_Jour'] = df_produit['Ventes_Jour'].interpolate()
df_produit['Promo'] = df_produit['Promo'].fillna(0)
# 'method=ffill' est déprécié, on utilise directement .ffill()
df_produit['Prix'] = df_produit['Prix'].ffill()

# ==========================================
# 2. FEATURE ENGINEERING (Création de variables)
# ==========================================
df_produit['Jour_Semaine_Num'] = df_produit.index.dayofweek
df_produit['Mois_Num'] = df_produit.index.month

X_brut = df_produit[['Promo', 'Prix', 'Jour_Semaine_Num', 'Mois_Num']]
y = df_produit['Ventes_Jour']

X = pd.get_dummies(X_brut, columns=['Jour_Semaine_Num', 'Mois_Num'], drop_first=True)

# ==========================================
# 3. Division Temporelle (Train / Validation / Test)
# ==========================================
jours_test = 30
jours_val = 30

train_y = y.iloc[:-(jours_test + jours_val)]
train_X = X.iloc[:-(jours_test + jours_val)]

val_y = y.iloc[-(jours_test + jours_val):-jours_test]
val_X = X.iloc[-(jours_test + jours_val):-jours_test]

test_y = y.iloc[-jours_test:]
test_X = X.iloc[-jours_test:]

print(f"--- DÉCOUPAGE DE LA BASE DE DONNÉES ---")
print(f"Taille Entraînement : {len(train_y)} jours")
print(f"Taille Validation   : {len(val_y)} jours")
print(f"Taille Test         : {len(test_y)} jours\n")

# ==========================================
# 4. Modélisation : Entraînement et Validation
# ==========================================
modele_lr = LinearRegression()
modele_lr.fit(train_X, train_y)
pred_val = modele_lr.predict(val_X)

# ==========================================
# 5. Modélisation : Mise à jour et Test Final
# ==========================================
X_train_et_val = pd.concat([train_X, val_X])
y_train_et_val = pd.concat([train_y, val_y])

modele_lr.fit(X_train_et_val, y_train_et_val)
pred_test = modele_lr.predict(test_X)

pred_val = pd.Series(pred_val, index=val_y.index)
pred_test = pd.Series(pred_test, index=test_y.index)

# ==========================================
# 6. Évaluation de la précision (CORRECTION DE L'INDENTATION)
# ==========================================
def afficher_precision(y_vrai, y_pred, nom_phase):
    mae = mean_absolute_error(y_vrai, y_pred)
    mape = mean_absolute_percentage_error(y_vrai, y_pred) * 100
    precision_globale = 100 - mape

    print(f"--- RÉSULTATS : {nom_phase} ---")
    print(f"Marge d'erreur (MAE) : ±{mae:.2f} articles")
    print(f"Erreur moyenne (MAPE): {mape:.2f} %")
    print(f"🎯 Précision du modèle : {precision_globale:.2f} %\n")

afficher_precision(val_y, pred_val, "PHASE DE VALIDATION")
afficher_precision(test_y, pred_test, "PHASE DE TEST FINAL")

# ==========================================
# 7. Visualisation
# ==========================================
plt.figure(figsize=(15, 6))

plt.plot(train_y.index[-90:], train_y.iloc[-90:], label='Entraînement', color='#1f77b4')
plt.plot(val_y.index, val_y, label='Réel (Validation)', color='#ff7f0e')
plt.plot(val_y.index, pred_val, label='Prédictions Régression (Val)', color='#ff7f0e', linestyle='--')
plt.plot(test_y.index, test_y, label='Réel (Test)', color='#2ca02c')
plt.plot(test_y.index, pred_test, label='Prédictions Régression (Test)', color='#d62728', linestyle='--')

plt.axvline(x=val_y.index[0], color='black', linestyle=':', alpha=0.5)
plt.axvline(x=test_y.index[0], color='black', linestyle=':', alpha=0.5)

plt.title("Prévision de la demande (Régression Linéaire)", fontsize=14, fontweight='bold')
plt.xlabel("Date", fontsize=12)
plt.ylabel("Unités Vendues", fontsize=12)
plt.legend()
plt.grid(True, linestyle=':', alpha=0.6)
plt.tight_layout()
plt.show()

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from prophet import Prophet
from sklearn.metrics import mean_absolute_error, mean_squared_error, mean_absolute_percentage_error
import warnings
import logging

# Désactiver les messages de log inutiles de Prophet
logging.getLogger('prophet').setLevel(logging.WARNING)
warnings.filterwarnings("ignore")

# ==========================================
# 1. Chargement hyper-robuste
# ==========================================
fichier = 'boissons.xlsx'  # Modifie ici avec ton nom exact de fichier

# Tente de lire en Excel, sinon bascule automatiquement sur CSV
try:
    df = pd.read_excel(fichier)
except Exception:
    df = pd.read_csv(fichier)

# Nettoyage extrême des noms de colonnes (enlève les espaces invisibles)
df.columns = df.columns.str.strip()

try:
    df['Date'] = pd.to_datetime(df['Date'], format='mixed', dayfirst=True)
    # Prophet déteste les fuseaux horaires (Timezones), on s'assure de les enlever :
    df['Date'] = df['Date'].dt.tz_localize(None)
except KeyError:
    raise KeyError(f"Colonne 'Date' introuvable. Voici les colonnes détectées : {df.columns.tolist()}")

# ==========================================
# 2. Filtrage et Nettoyage du produit
# ==========================================
# On s'assure qu'il n'y a pas d'espaces dans la colonne des ID
df['Produit_ID'] = df['Produit_ID'].astype(str).str.strip()

produit_cible = 'P001'
df_produit = df[df['Produit_ID'] == produit_cible].copy()

if df_produit.empty:
    raise ValueError(f"🛑 ERREUR : Aucune donnée trouvée pour le produit {produit_cible}. Vérifie l'ID.")

df_produit.sort_values('Date', inplace=True)

# Remplissage robuste des valeurs manquantes (pour ne laisser AUCUN "NaN" qui ferait crasher Prophet)
df_produit['Ventes_Jour'] = df_produit['Ventes_Jour'].interpolate().bfill().ffill()

if 'Promo' in df_produit.columns:
    df_produit['Promo'] = df_produit['Promo'].fillna(0)
if 'Prix' in df_produit.columns:
    df_produit['Prix'] = df_produit['Prix'].ffill().bfill()

# ==========================================
# 3. Formatage Spécifique pour PROPHET
# ==========================================
# Prophet exige exactement les colonnes 'ds' et 'y'
colonnes_a_garder = ['Date', 'Ventes_Jour']
if 'Promo' in df_produit.columns: colonnes_a_garder.append('Promo')
if 'Prix' in df_produit.columns: colonnes_a_garder.append('Prix')

df_prophet = df_produit[colonnes_a_garder].copy()
df_prophet.rename(columns={'Date': 'ds', 'Ventes_Jour': 'y'}, inplace=True)

# Nettoyage ultime
df_prophet.dropna(inplace=True)

# ==========================================
# 4. Division Temporelle (Train / Validation / Test)
# ==========================================
jours_test = 30
jours_val = 30

train_df = df_prophet.iloc[:-(jours_test + jours_val)]
val_df = df_prophet.iloc[-(jours_test + jours_val):-jours_test]
test_df = df_prophet.iloc[-jours_test:]

print(f"--- DÉCOUPAGE DE LA BASE DE DONNÉES ---")
print(f"Taille Entraînement : {len(train_df)} jours")
print(f"Taille Validation   : {len(val_df)} jours")
print(f"Taille Test         : {len(test_df)} jours\n")

# ==========================================
# 5. Modélisation Prophet (Entraînement & Validation)
# ==========================================
print("Entraînement du modèle Prophet en cours...")
modele_prophet_val = Prophet(daily_seasonality=False, yearly_seasonality=False)

if 'Promo' in df_prophet.columns: modele_prophet_val.add_regressor('Promo')
if 'Prix' in df_prophet.columns: modele_prophet_val.add_regressor('Prix')

modele_prophet_val.fit(train_df)

# Préparation des données futures pour la prédiction
cols_pred = ['ds']
if 'Promo' in val_df.columns: cols_pred.append('Promo')
if 'Prix' in val_df.columns: cols_pred.append('Prix')

forecast_val = modele_prophet_val.predict(val_df[cols_pred])
pred_val = forecast_val['yhat'].values

# ==========================================
# 6. Modélisation : Mise à jour et Test Final
# ==========================================
train_et_val_df = pd.concat([train_df, val_df])

modele_prophet_test = Prophet(daily_seasonality=False, yearly_seasonality=False)
if 'Promo' in df_prophet.columns: modele_prophet_test.add_regressor('Promo')
if 'Prix' in df_prophet.columns: modele_prophet_test.add_regressor('Prix')

modele_prophet_test.fit(train_et_val_df)

forecast_test = modele_prophet_test.predict(test_df[cols_pred])
pred_test = forecast_test['yhat'].values

# ==========================================
# 7. Évaluation de la précision
# ==========================================
def afficher_precision(y_vrai, y_pred, nom_phase):
    mae = mean_absolute_error(y_vrai, y_pred)
    mape = mean_absolute_percentage_error(y_vrai, y_pred) * 100
    precision_globale = 100 - mape

    print(f"--- RÉSULTATS : {nom_phase} ---")
    print(f"Marge d'erreur (MAE) : ±{mae:.2f} articles")
    print(f"Erreur moyenne (MAPE): {mape:.2f} %")
    print(f"🎯 Précision du modèle : {precision_globale:.2f} %\n")

afficher_precision(val_df['y'].values, pred_val, "PHASE DE VALIDATION")
afficher_precision(test_df['y'].values, pred_test, "PHASE DE TEST FINAL")

# ==========================================
# 8. Visualisation
# ==========================================
plt.figure(figsize=(15, 6))

plt.plot(train_df['ds'][-90:], train_df['y'][-90:], label='Entraînement', color='#1f77b4')
plt.plot(val_df['ds'], val_df['y'], label='Réel (Validation)', color='#ff7f0e')
plt.plot(val_df['ds'], pred_val, label='Prédictions Prophet (Val)', color='#ff7f0e', linestyle='--')
plt.plot(test_df['ds'], test_df['y'], label='Réel (Test)', color='#2ca02c')
plt.plot(test_df['ds'], pred_test, label='Prédictions Prophet (Test)', color='#d62728', linestyle='--')

plt.axvline(x=val_df['ds'].iloc[0], color='black', linestyle=':', alpha=0.5)
plt.axvline(x=test_df['ds'].iloc[0], color='black', linestyle=':', alpha=0.5)

plt.title("Prévision de la demande (Modèle Facebook Prophet)", fontsize=14, fontweight='bold')
plt.xlabel("Date", fontsize=12)
plt.ylabel("Unités Vendues", fontsize=12)
plt.legend()
plt.grid(True, linestyle=':', alpha=0.6)
plt.tight_layout()
plt.show()

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from pmdarima import auto_arima
from sklearn.metrics import mean_absolute_error, mean_squared_error, mean_absolute_percentage_error
import warnings

warnings.filterwarnings("ignore")

# ==========================================
# 1. Chargement hyper-robuste
# ==========================================
fichier = 'boissons.xlsx'

try:
    df = pd.read_excel(fichier)
except Exception:
    df = pd.read_csv(fichier)

df.columns = df.columns.str.strip()

try:
    df['Date'] = pd.to_datetime(df['Date'], format='mixed', dayfirst=True).dt.tz_localize(None)
except KeyError:
    raise KeyError(f"Colonne 'Date' introuvable. Colonnes détectées : {df.columns.tolist()}")

# ==========================================
# 2. Filtrage et Nettoyage du produit
# ==========================================
df['Produit_ID'] = df['Produit_ID'].astype(str).str.strip()
produit_cible = 'P001'

df_produit = df[df['Produit_ID'] == produit_cible].copy()

if df_produit.empty:
    raise ValueError(f"🛑 ERREUR : Aucune donnée pour le produit {produit_cible}.")

df_produit.sort_values('Date', inplace=True)
df_produit.set_index('Date', inplace=True)
df_produit = df_produit.asfreq('D')

df_produit['Ventes_Jour'] = df_produit['Ventes_Jour'].interpolate().bfill().ffill()

cols_exogenes = []
if 'Promo' in df_produit.columns:
    df_produit['Promo'] = df_produit['Promo'].fillna(0)
    cols_exogenes.append('Promo')
if 'Prix' in df_produit.columns:
    df_produit['Prix'] = df_produit['Prix'].ffill().bfill()
    cols_exogenes.append('Prix')

y = df_produit['Ventes_Jour']
X = df_produit[cols_exogenes] if cols_exogenes else None

# ==========================================
# 3. Division Temporelle (Train / Validation / Test)
# ==========================================
jours_test = 30
jours_val = 30

train_y = y.iloc[:-(jours_test + jours_val)]
val_y = y.iloc[-(jours_test + jours_val):-jours_test]
test_y = y.iloc[-jours_test:]

if X is not None:
    train_X = X.iloc[:-(jours_test + jours_val)]
    val_X = X.iloc[-(jours_test + jours_val):-jours_test]
    test_X = X.iloc[-jours_test:]
else:
    train_X = val_X = test_X = None

print(f"--- DÉCOUPAGE DE LA BASE DE DONNÉES ---")
print(f"Taille Entraînement : {len(train_y)} jours")
print(f"Taille Validation   : {len(val_y)} jours")
print(f"Taille Test         : {len(test_y)} jours\n")

# ==========================================
# 4. Modélisation ARIMA (STRICT)
# ==========================================
print("Recherche des meilleurs paramètres ARIMA en cours...")
# C'est ici que la magie opère pour rester sur un ARIMA simple
modele_arima = auto_arima(
    y=train_y,
    X=train_X,
    seasonal=False,
    # m=7 a été supprimé
    trace=False,
    error_action='ignore',
    suppress_warnings=True,
    stepwise=True
)

print(f"Meilleur modèle ARIMA trouvé : {modele_arima.order}")

pred_val = modele_arima.predict(n_periods=jours_val, X=val_X)

# ==========================================
# 5. Modélisation : Mise à jour et Test Final
# ==========================================
modele_arima.update(val_y, X=val_X)
pred_test = modele_arima.predict(n_periods=jours_test, X=test_X)

# ==========================================
# 6. Évaluation de la précision
# ==========================================
def afficher_precision(y_vrai, y_pred, nom_phase):
    mae = mean_absolute_error(y_vrai, y_pred)
    mape = mean_absolute_percentage_error(y_vrai, y_pred) * 100
    precision_globale = 100 - mape

    print(f"--- RÉSULTATS : {nom_phase} ---")
    print(f"Marge d'erreur (MAE) : ±{mae:.2f} articles")
    print(f"Erreur moyenne (MAPE): {mape:.2f} %")
    print(f"🎯 Précision du modèle : {precision_globale:.2f} %\n")

afficher_precision(val_y, pred_val, "PHASE DE VALIDATION")
afficher_precision(test_y, pred_test, "PHASE DE TEST FINAL")

# ==========================================
# 7. Visualisation
# ==========================================
plt.figure(figsize=(15, 6))

plt.plot(train_y.index[-90:], train_y.iloc[-90:], label='Entraînement', color='#1f77b4')
plt.plot(val_y.index, val_y, label='Réel (Validation)', color='#ff7f0e')
plt.plot(val_y.index, pred_val, label='Prédictions ARIMA (Val)', color='#ff7f0e', linestyle='--')
plt.plot(test_y.index, test_y, label='Réel (Test)', color='#2ca02c')
plt.plot(test_y.index, pred_test, label='Prédictions ARIMA (Test)', color='#d62728', linestyle='--')

plt.axvline(x=val_y.index[0], color='black', linestyle=':', alpha=0.5)
plt.axvline(x=test_y.index[0], color='black', linestyle=':', alpha=0.5)

plt.title("Prévision de la demande (Modèle ARIMA strict)", fontsize=14, fontweight='bold')
plt.xlabel("Date", fontsize=12)
plt.ylabel("Unités Vendues", fontsize=12)
plt.legend()
plt.grid(True, linestyle=':', alpha=0.6)
plt.tight_layout()
plt.show()